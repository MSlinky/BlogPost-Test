# BlogPost-Test

Instalar cliente
  <br/> Instalar dependencias
  <br/> Desde la carpeta client
  
    /client
      npm install
    
   Ejecutar:
    
    run:
      npm run dev

Instalar servidor
	<br/> Instalar dependencias
  <br/> Desde la carpeta server
  
    /server
      npm install
  
  Ejecutar:
  
    run:
      npm start 
